import React from 'react';

function Home({Data}) {
    
    let datafilter = Data.filter((fd, fi) => fd.salary > 25000);
    
    return (
        <div>
            {
                
            }
        </div>
    );
}

export default Home;